> מסמך תיאור פרויקט ועקרונות ליבה למערכת פורטל המשלחות - עודכן לארכיטקטורת Vercel (ספטמבר 2026).
>
> ⚠️ **הערת מיקום קריטית לסביבת הפיתוח וה-AI:**
> תיקיית העבודה **היחידה והבלעדית** לכל שינוי, פיתוח, עריכה ועדכון שוטף של הפרויקט היא אך ורק:
> `C:\Users\Shachar Lavi\Desktop\IMSF 26 VERCEL` (או קיצור הדרך שלה בדסקטופ).
> התיקייה הישנה `IMSF 26` הוגדרה כבלתי פעילה ואין לגשת אליה או לערוך בה קבצים.

---

### פרק 1. מהות הפרויקט וקהל היעד

**פסטיבל חשיפה בינלאומית למוזיקה (Exposure Festival 2026 / IMSF 26)** הוא פרויקט הדגל של הצוללת הצהובה, המביא ארצה עשרות מקבלי החלטות מתעשיית המוזיקה העולמית (מנהלי פסטיבלים, מפיקים, בוקרים, ועיתונאי מוזיקה VIP) לסיור הופעות וכיתות אמן בארץ.

לקראת הפסטיבל, מקבלי ההחלטות (להלן: **אורחים**), נדרשים להזין נתונים אישיים למערכת (פרטי דרכון, טיסות, תמונת פספורט לטובת תגי שם, וטקסט ביוגרפי) שישמשו את ההפקה בארגון הסיור.

---

### פרק 2. מטרת הפורטל: ממשק עבודה אסינכרוני ואלגנטי

הפורטל נועד להחליף (או להשלים) את שיטת העבודה הישנה מול האורחים:

* **הפרדה בין שרת ללקוח (Vercel + Google API):** האתר הוא אפליקציית דפדפן מודרנית (Vite/Vanilla JS) הנטענת משרתי Vercel. מאגר הנתונים נשאר ב-Google Sheets, כאשר קוד ה-Apps Script מתפקד אך ורק כ-API (ממשק) המקבל ומחזיר נתונים ב-JSON.
* **אסינכרוניות וחיסכון בזמן:** האורח יכול להיכנס בכל שלב, לראות מה חסר לו, ולהשלים את החסר.
* **חווית משתמש:** ממשק חלק, מגיב מהר, בלי ריענוני עמוד, וידידותי למובייל.

---

### פרק 3. גורמים מעורבים וממשקי עבודה

* **שחר (מנהל ההפקה):** מנהל את הנתונים בגיליון.
* **ענת (מנהלת הטיסות):** מנהלת את הטיסות בגיליון נפרד.
* **האורחים (Delegates / VIPs):** משתמשי הקצה של האתר המזינים את המידע.

---

### פרק 4. עקרונות ברזל בארכיטקטורה וקוד

* **Google Sheets כמקור אמת יחיד (Single Source of Truth - SSOT):** כל נתון (דרכון, טיסות, ביוגרפיה) קיים רק בגיליון האמת.
* **הפרדת סביבות (Production vs Sandbox):**
    * **סביבת אמת:** אתר Vercel הראשי (ענף main) השואב מגיליון האמת.
    * **סביבת ניסוי:** אתר Vercel המדומה (ענף sandbox) השואב מגיליון הניסוי. הפיתוח נעשה כאן.
* **עיצוב ו-UI (Strict UI Freeze):** יש לשמור על העיצוב הקיים בהתאם למפורט ב-DESIGN_RULES.md.
* **פיתוח Mobile-First:** כל אלמנט נבדק קודם במובייל ורק אז מותאם לדסקטופ.
* **אנגלית מוחלטת ב-UI:** אין עברית בממשק המשתמש של האתר.
* **פרטיות אורחים:** אורח נחשף אך ורק לנתונים שלו.
