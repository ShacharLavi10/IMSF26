# PROJECT_CONTEXT.md

> מסמך תשתית וחזון מערכתי עבור סביבת הפיתוח וה-AI של Anti-Gravity.

---

### 🌟 1. חזון ומטרת העל של הפרויקט

**אירועי חשיפה בינלאומית למוזיקה (Exposure Festival 2026 / IMSF 26)** הם פרויקט דגל לאומי ותרבותי, שמטרתו לחבר בין עשרות אנשי מפתח מתעשיית המוזיקה העולמית (מנהלי פסטיבלים בינלאומיים, לייבלים, בוקרים, עיתונאים ומשלחות VIP) לבין סצנת המוזיקה המקומית בירושלים ובתל אביב.

הפרויקט מתאפיין בקצב מהיר, משתנים לוגיסטיים מורכבים (טיסות גלובליות, הסעות, לילות במלונות שונים לפי ערים, צרכים מיוחדים ותוכן אמנותי), ודורש סטנדרט אירוח ומקצועיות ברמה הגבוהה ביותר.

---

### 🎯 2. ייעוד התפקיד: ניהול אירוח ולוגיסטיקת משלחות

ניהול האירוח והלוגיסטיקה (בראשות **שחר לביא**) הוא הלב המבצעי של הפסטיבל:

* **חוויית אורח ללא רבב:** מתן מענה יוקרתי, מדויק ואישי לכל אורח בינלאומי מרגע הזמנתו ועד עזיבתו.
* **אינטגרציה ושליטה בזמן אמת:** שליטה מלאה בשינויי טיסות, צ'ק-אין/צ'ק-אאוט במלונות, מסמכים רשמיים (דרכונים, תמונות, ביוגרפיות) וטפסי העדפות.
* **שקט תפעולי להפקה:** אוטומציה של תהליכי הסנכרון, מניעת כפילויות, צמצום טעויות אנוש ומעקב שוטף ללא חיכוך.

---

### 👥 3. דמויות מפתח וממשקי עבודה מרכזיים

* **שחר לביא:** מנהל האירוח, הלוגיסטיקה ואינטגרציית מערכות המידע והאוטומציה. מוביל את אפיון התהליכים, הפורטל והסנכרונים.
* **ענת (רכזת טיסות וסוכנות נסיעות):** מנהלת גיליון חיצוני נפרד שבו מרוכזים ומתעדכנים פרטי הטיסות בזמן אמת.
* **צוות הפקת הפסטיבל:** גורמי הפקה שמתפעלים שיבוצי מלונות בירושלים ובתל אביב, אישורי הגעה, הסעות ותוכניות אמנותיות.
* **האורחים הבינלאומיים (Delegates / VIPs):** משתמשי הקצה של הפורטל הדיגיטלי (גישה מותאמת אישית מבוססת מייל).

---

### 🧠 4. עקרונות תכנון ודרכי חשיבה מובילות (Core Principles)

* **גיליון "אורחים" כמקור האמת היחיד (Single Source of Truth - SSOT):**
כל נתון זהות (שם, מייל, שיוך, סטטוס אישורים) נקבע ומנוהל בגיליון הראשי בלבד. שאר הגיליונות הם נגזרות משקפות.
* **צימוד חד-חד-ערכי לפי מייל (Email-Centric Coupling):**
המייל הוא המזהה הייחודי (Unique ID) של כל אורח. סדר השורות בכל הגיליונות (כולל גיליונות מלונות וגיליון הטיסות החיצוני של ענת) מוכתב ומיושר תמיד לפי סדר הופעת המיילים בגיליון הראשי.
* **ניקוי אוטומטי של שורות יתומות (Data Hygiene):**
מחיקת אורח מהגיליון הראשי גורמת להסרתו המיידית והאוטומטית מכל גיליונות הלוויין ומהשיקופים, ללא השארת שורות ריקות או שאריות מידע.
* **הפרדה קשיחה בין לוגיקה לעיצוב (Strict UI Freeze):**
עיצוב הפורטל, המיתוג, הטיפוגרפיה והטקסטים מוגדרים ונעולים ב-`DESIGN_RULES.md`. שינויי מערכת עוסקים אך ורק בלוגיקת השרת (Backend), בסנכרון ובשליפת המידע, ללא שינוי ה-UI אלא אם התקבלה דרישה מפורשת.
* **חוויית Mobile-First אבסולוטית (Mobile-First Philosophy):**
כל תכנון ועיצוב של רכיבי הפורטל (דפים, כפתורים, טיפוגרפיה, טפסים ומידע) מבוצעים בראש ובראשונה עבור מסכי טלפון נייד (Mobile-First). נקודת המוצא הקבועה היא שהאורחים מתפעלים את הפורטל מהסמארטפון תוך כדי תנועה בפסטיבל, ורק לאחר מכן מוודאים התאמה למסכי מחשב.
* **ממשק באנגלית בלבד (Strict English-Only UI):**
כל הטקסטים, המלל, הכותרות, הכפתורים, ההודעות והשדות בפורטל האורחים יהיו **תמיד באנגלית בלבד** (ללא עברית כלל בממשק האורח). כל תוכן או עדכון ינוסח באנגלית בינלאומית, רהוטה ומקצועית. בכל מקרה של ספק או התלבטות לגבי ניסוח כותרות או תוכן – יש להעלות שאלה ולהתייעץ, אך הכלל הקשיח הוא אנגלית בלבד.
* **מידור מידע ופרטיות האורח (Guest Privacy Isolation):**
בפורטל האורח יוצגו אך ורק שדות המיועדים לעיניו (טיסות אישיות מסוננות, שיבוץ מלון, צ'קליסט סטטוס אישי וספר האורחים הפומבי). שדות הערות פנימיים ונתוני תפעול של ההפקה נשארים מוסתרים.

---

### 🏗️ 5. תמונת המצב הנוכחית: מה נבנה עד כה?

1. **מנוע סנכרון רב-טבלאי אוטומטי (`SyncEngine.gs`):**
* סנכרון מלא ואיחוד שורות בין הגיליון הראשי לגיליונות `מלונות ירושלים` ו-`מלונות תל אביב`.
* סנכרון ומיון של גיליון הטיסות החיצוני של ענת (`עדכוני טיסות`) תוך שימוש בנוסחאות דינמיות (`XLOOKUP` ו-`IMPORTRANGE`).
* עדכון אוטומטי באירועי עריכה ושינוי (`onEdit`, `onChange`).


2. **גיליון שיקוף טיסות פנימי (`טיסות - שיקוף`):**
* שיקוף נתוני הטיסות לצורכי הפורטל ומעקב פנימי מהיר.


3. **פורטל אורחים דיגיטלי (`PortalBackend.gs` + `Index.html`):**
* כניסה מהירה ומאובטחת באמצעות כתובת מייל רשומה בלבד.
* צ'קליסט סטטוס משימות חי (טופס טיסות, טופס מלון, ביוגרפיה, דרכון, תמונה).
* ממשק העלאת קבצים ישירה ל-Google Drive (דרכונים ותמונות אישיות לתיקיות ייעודיות).
* ממשק עדכון ביוגרפיה באנגלית ישירות לגיליון הראשי.
* תצוגת לוח טיסות ושיבוצי חדרים/מלונות לפי עיר.
* ספר אורחים (Delegates Directory) לנטוורקינג בין משתתפי הפסטיבל.


4. **ארכיטקטורה מודולרית וחיבור סביבת פיתוח:**
* חלוקת הקוד לרכיבים: `Config.gs`, `SyncEngine.gs`, `PortalBackend.gs`, `FileServices.gs`.
* חיבור מלא ל-Clasp לעבודה מקצועית ישירה ללא העתק-הדבק ידני.



---

### 🗺️ 6. יעדים ומשימות להמשך הפיתוח (Roadmap)

* [ ] **הגדרת Installable Triggers:** לוודא שהטריגרים של הסנכרון והחישוב פועלים ברקע בהרשאות מלאות עבור כל משתמשי ההפקה.
* [ ] **מערך התראות והודעות (הפקה):** מנגנון מעקב והתרעה על אורחים שלא השלימו טפסים/קבצים לקראת מועדי היעד.
* [ ] **הפורטל כאפליקציית "Live Companion":** שילוב מערכת הודעות בזמן אמת בפורטל האורחים. הצגת לו"ז יומי, הודעות מערכת כלליות (לכלל המשלחות) והודעות אישיות ישירות בממשק המשתמש כדי למנוע "רעש" בקבוצות הווטסאפ.
* [ ] **בדיקות עומס ודיבאגינג תחת שינויים קיצוניים:** בדיקת תרחישי קצה של הזזת שורות, מחיקות קבוצתיות ועדכוני טיסות סימולטניים.
* [ ] **בקרת שגיאות משתמש (Error Handling):** שיפור משובים ויזואליים בפורטל במקרה של קבצים כבדים מדי או ניתוקי רשת.

---

### 🔄 7. מתודולוגיית עבודה ואישור שינויים (Development Workflow)

כדי לשמור על יציבות המערכת ולמנוע טעויות בסביבת האמת, כל שינוי בקוד יבוצע על פי זרימת העבודה הבאה:
1. **הטמעה ראשונית במדומה (Sandbox):** השינוי יוטמע תחילה אך ורק בסביבה המדומה (הסקריפט המקושר ל-Sandbox).
2. **בדיקת פריסה (Test Deployment):** אשלח לך לינק של בדיקת פריסה (Test Deployment) מתוך הסקריפט המדומה כדי שתוכל לבחון את התוצאה בזמן אמת.
3. **איטרציות ודיוק:** במידה ונדרשים שינויים נוספים (משהו לא עבד או לא אהבת את התוצאה), נתקן, נעדכן את הקוד ונבדוק שוב דרך הלינק.
4. **אישור, עדכון סופי ופריסה (Approval & Final Deploy):** **אך ורק** לאחר שתהיה מרוצה לחלוטין ותאשר שהשינוי מוכן, אעדכן סופית את כל הקבצים הלוקאליים ואדחוף אותם גם לפרויקט המדומה וגם לפרויקט האמיתי (Production).
5. **שמירה על הלינקים הקיימים (Deployment Versioning):** **תמיד** בעת עדכון פריסה רשמית, העדכון יבוצע כיצירת "גרסה חדשה" (New Version) על גבי הפריסה הקיימת, כדי להבטיח שלינק הגישה של האורחים לפורטל **לעולם לא ישתנה**.

---

## 🏗️ ארכיטקטורת המערכת החדשה (החל מספטמבר 2026)

המערכת עברה שדרוג משמעותי והופרדה (Decoupled) לשני חלקים עצמאיים כדי לאפשר גמישות, מהירות וניהול תקין של סביבות:

### 1. צד לקוח (Frontend) - Vercel
*   **תשתית:** האתר מבוסס על סביבת קוד פתוח מודרנית (Vite - Vanilla JS) ומאוחסן בשרתי Vercel.
*   **אין יותר HTML Service:** האתר כבר לא נטען דרך doGet() של גוגל, אלא יושב בנפרד כאתר לכל דבר עם דומיין של Vercel.
*   **הפרדת סביבות מוחלטת:**
    *   **סביבת Production (האתר האמיתי):** מקושרת לענף ה-main ב-GitHub, ושואבת את הנתונים מה-API של גיליון ה-Production.
    *   **סביבת Sandbox (אתר הטסטים):** מקושרת לענף ה-sandbox ב-GitHub (אליו מועלים עדכוני הפיתוח), ושואבת את הנתונים מה-API של גיליון ה-Sandbox.

### 2. צד שרת (Backend) - Google Apps Script כ-API
*   **ממשק API:** קובץ ה-PortalBackend.gs הוסב ל-REST API "עיוור" (Headless). הוא משתמש בפונקציית doPost(e) שמקבלת בקשות JSON (המוסוות כ-	ext/plain כדי לעקוף חסימות CORS), מריצה את הפונקציה המבוקשת, ומחזירה JSON עם התשובה.
*   **פוליפיל (Polyfill):** כל לוגיקת צד-הלקוח המקורית שמשתמשת ב-google.script.run נשמרה במלואה הודות לקובץ main.js. קובץ זה "לוכד" את כל הקריאות הישנות ומתרגם אותן אוטומטית לבקשות etch תקניות אל מול כתובת ה-API הרלוונטית (המוגדרת במשתנה הסביבה VITE_GAS_API_URL).

### 3. תהליך הפיתוח (Workflow) החדש:
1.  **פיתוח וקוד:** הסוכן (AI) מבצע שינויים בקוד צד הלקוח או השרת.
2.  **דחיפה למדומה:** הקוד החדש נדחף אל ענף ה-sandbox ב-GitHub. 
3.  **בדיקה:** Vercel מעדכנת אוטומטית את האתר המדומה (imsf26-sandbox). הבדיקות מבוצעות בלינק זה מול נתונים מדומים.
4.  **אישור ושחרור ל-Production:** לאחר אישור, הקוד ממוזג לענף ה-main, ו-Vercel מפיצה את העדכון לאתר הרשמי החי.

