# PROJECT_CONTEXT.md

> ⚠️ **הערת מיקום קריטית לסביבת הפיתוח וה-AI:**
> תיקיית העבודה **היחידה והבלעדית** לכל שינוי, פיתוח, עריכה ועדכון שוטף של הפרויקט היא אך ורק:
> `C:\Users\Shachar Lavi\Desktop\IMSF 26 VERCEL`
> (התיקייה הישנה `IMSF 26` הוגדרה כבלתי פעילה ואין לגשת אליה או לערוך בה קבצים).

---

### 🌟 1. חזון ומטרת העל של הפרויקט

**אירועי חשיפה בינלאומית למוזיקה (Exposure Festival 2026 / IMSF 26)** הם פרויקט דגל לאומי ותרבותי של הצוללת הצהובה, שמטרתו לחבר בין עשרות אנשי מפתח מתעשיית המוזיקה העולמית (מנהלי פסטיבלים בינלאומיים, לייבלים, בוקרים, עיתונאים ומשלחות VIP) לבין סצנת המוזיקה המקומית בירושלים ובתל אביב.

הפרויקט מתאפיין בקצב מהיר, משתנים לוגיסטיים מורכבים (טיסות גלובליות, הסעות, לילות במלונות שונים לפי ערים, צרכים מיוחדים ותוכן אמנותי), ודורש סטנדרט אירוח ומקצועיות ברמה הגבוהה ביותר.

---

### 🎯 2. ייעוד התפקיד: ניהול אירוח ולוגיסטיקת משלחות

ניהול האירוח והלוגיסטיקה (בראשות **שחר לביא**) הוא הלב המבצעי של הפסטיבל:

* **חוויית אורח ללא רבב:** מתן מענה יוקרתי, מדויק ואישי לכל אורח בינלאומי מרגע הזמנתו ועד עזיבתו.
* **אינטגרציה ושליטה בזמן אמת:** שליטה מלאה בשינויי טיסות, צ'ק-אין/צ'ק-אאוט במלונות, מסמכים רשמיים (דרכונים, תמונות, ביוגרפיות) וטפסי העדפות.
* **שקט תפעולי להפקה:** אוטומציה של תהליכי הסנכרון, מניעת כפילויות, צמצום טעויות אנוש ומעקב שוטף ללא חיכוך.

---

### 👥 3. דמויות מפתח וממשקי עבודה מרכזיים

* **שחר לביא:** מנהל האירוח, הלוגיסטיקה ואינטגרציית מערכות המידע והאוטומציה. מוביל את אפיון התהליכים, הפורטל והסנכרונים.
* **ענת (רכזת טיסות וסוכנות נסיעות):** מנהלת גיליון חיצוני נפרד שבו מרוכזים ומתעדכנים פרטי הטיסות בזמן אמת.
* **צוות הפקת הפסטיבל:** גורמי הפקה שמתפעלים שיבוצי מלונות בירושלים ובתל אביב, אישורי הגעה, הסעות ותוכניות אמנותיות.
* **האורחים הבינלאומיים (Delegates / VIPs):** משתמשי הקצה של הפורטל הדיגיטלי (גישה מותאמת אישית מבוססת מייל וקוד OTP).

---

### 🧠 4. עקרונות תכנון ודרכי חשיבה מובילות (Core Principles)

* **גיליון "אורחים" כמקור האמת היחיד (Single Source of Truth - SSOT):**
  כל נתון זהות (שם, מייל, שיוך, סטטוס אישורים) נקבע ומנוהל בגיליון הראשי בלבד. שאר הגיליונות הם נגזרות משקפות.
* **צימוד חד-חד-ערכי לפי מייל (Email-Centric Coupling):**
  המייל הוא המזהה הייחודי (Unique ID) של כל אורח. סדר השורות בכל הגיליונות מוכתב ומיושר תמיד לפי סדר הופעת המיילים בגיליון הראשי.
* **ניקוי אוטומטי של שורות יתומות (Data Hygiene):**
  מחיקת אורח מהגיליון הראשי גורמת להסרתו המיידית והאוטומטית מכל גיליונות הלוויין ומהשיקופים, ללא השארת שורות ריקות או שאריות מידע.
* **הפרדה קשיחה בין לוגיקה לעיצוב (Strict UI Freeze):**
  עיצוב הפורטל, המיתוג, הטיפוגרפיה והטקסטים מוגדרים ונעולים ב-`DESIGN_RULES.md`. שינויי מערכת עוסקים אך ורק בלוגיקה, בסנכרון ובשליפת המידע, ללא שינוי ה-UI אלא אם התקבלה דרישה מפורשת.
* **חוויית Mobile-First אבסולוטית (Mobile-First Philosophy):**
  כל תכנון ועיצוב של רכיבי הפורטל (דפים, כפתורים, טיפוגרפיה, טפסים ומידע) מבוצעים בראש ובראשונה עבור מסכי טלפון נייד (Mobile-First). נקודת המוצא הקבועה היא שהאורחים מתפעלים את הפורטל מהסמארטפון תוך כדי תנועה בפסטיבל, ורק לאחר מכן מוודאים התאמה למסכי מחשב.
* **ממשק באנגלית בלבד (Strict English-Only UI):**
  כל הטקסטים, המלל, הכותרות, הכפתורים, ההודעות והשדות בפורטל האורחים יהיו **תמיד באנגלית בלבד** (ללא עברית כלל בממשק האורח). כל תוכן או עדכון ינוסח באנגלית בינלאומית, רהוטה ומקצועית. בכל מקרה של ספק או התלבטות לגבי ניסוח כותרות או תוכן – יש להעלות שאלה ולהתייעץ, אך הכלל הקשיח הוא אנגלית בלבד.
* **מידור מידע ופרטיות האורח (Guest Privacy Isolation):**
  בפורטל האורח יוצגו אך ורק שדות המיועדים לעיניו (טיסות אישיות מסוננות, שיבוץ מלון, צ'קליסט סטטוס אישי וספר האורחים הפומבי). שדות הערות פנימיים ונתוני תפעול של ההפקה נשארים מוסתרים.

---

### 🏗️ 5. ארכיטקטורת המערכת (Decoupled Vercel + Google Apps Script API)

המערכת פועלת בארכיטקטורה מודרנית מופרדת (Decoupled):
1. **צד לקוח (Frontend) ב-Vercel:**
   * אפליקציית Web מודרנית ומהירה (HTML / CSS / Vanilla JS מבוססת Vite) המאוחסנת ב-Vercel.
   * חיבור ישיר ל-GitHub: ענף `main` מייצג את ה-Production (אתר האמת), וענף `sandbox` מייצג את סביבת הבדיקות.
2. **צד שרת (Backend API) ב-Google Apps Script:**
   * קובץ `PortalBackend-Vercel.gs` מתפקד כ-Headless REST API (`doPost`) שמחזיר ומקבל JSON.
   * קובץ `main.js` בצד הלקוח משמש כ-Polyfill שמתרגם את כל הקריאות לבקשות `fetch` מאובטחות אל כתובת ה-API הרלוונטית.
3. **מסד נתונים (Database):**
   * Google Sheets ממשיך לשמש כמסד הנתונים החי והנגיש עבור ההפקה.

---

### 🗺️ 6. יעדים ומשימות להמשך הפיתוח (Roadmap)

* [x] **הגדרת Installable Triggers:** הטריגרים של הסנכרון והחישוב פועלים ברקע בהרשאות מלאות עבור כל משתמשי ההפקה.
* [x] **הפרדת סביבות (Production vs. Sandbox):** סביבת אמת וסביבת ניסוי נפרדות לחלוטין (גיליונות, סקריפטים ודומיינים ב-Vercel).
* [x] **שדרוג מנגנון התחברות (OTP & Session):** התחברות מאובטחת באמצעות קוד OTP למייל ושמירת התחברות בדפדפן.
* [ ] **הפורטל כאפליקציית "Live Companion":** שילוב מערכת הודעות בזמן אמת בפורטל האורחים. הצגת לו"ז יומי, הודעות מערכת כלליות (לכלל המשלחות) והודעות אישיות ישירות בממשק המשתמש כדי למנוע "רעש" בקבוצות הווטסאפ.
* [ ] **מערך אישורי הגעה (RSVP):** מענה ואישור השתתפות לאירועים מיוחדים, ארוחות ערב ומפגשי VIP.
* [ ] **אימות והמרת לו"ז (Schedule Migration):** שילוב לו"ז הפסטיבל המלא מתוך גיליון האקסל אל תוך הפורטל.
* [ ] **בקרת שגיאות משתמש (Error Handling):** שיפור משובים ויזואליים בפורטל במקרה של קבצים כבדים מדי או ניתוקי רשת.

---

### 🔄 7. מתודולוגיית עבודה ואישור שינויים (Development Workflow)

כדי לשמור על יציבות המערכת ולמנוע טעויות בסביבת האמת, כל שינוי בקוד יבוצע על פי זרימת העבודה הבאה:
1. **פיתוח בלעדי בתיקיית `IMSF 26 VERCEL`:** כל שינוי בקוד (Frontend או Backend) מתבצע אך ורק בתיקייה זו.
2. **הטמעה ראשונית במדומה (Sandbox):**
   * **Frontend:** דחיפת השינוי ל-GitHub בענף `sandbox` לפריסה אוטומטית ב-Vercel תחת לינק ה-Preview הקבוע של הענף (`imsf26sandbox-git-sandbox-imsf-26.vercel.app`).
   * **Backend:** דחיפת הקוד לסביבת ה-Apps Script של ה-Sandbox.
3. **בדיקת פריסה ו-QA:** בדיקת הקוד החדש מתבצעת תמיד מול הלינק הקבוע של ענף ה-sandbox ב-Vercel כדי לשמור על רצף התחברות (ללא צורך בלוגין מחדש) וכדי ששחר יוכל לבחון את התוצאה בזמן אמת מול נתוני טסט.
4. **איטרציות ודיוק:** במידה ונדרשים שינויים נוספים, מתקנים ובודקים שוב על גבי לינק ענף ה-Sandbox.
5. **אישור, עדכון סופי ופריסה לאמת (Approval & Production Release):**
   * **אך ורק** לאחר קבלת אישור מפורש משחר:
   * **Frontend:** מיזוג (Merge) ענף ה-`sandbox` אל ענף ה-`main` ב-GitHub לשחרור מיידי ב-Vercel Production.
   * **Backend:** דחיפת קוד ה-Apps Script לסביבת ה-Production.
6. **שמירה על הלינקים הקיימים (Deployment Versioning):** **תמיד** בעת עדכון פריסה רשמית, העדכון יבוצע כיצירת "גרסה חדשה" (New Version) על גבי הפריסה הקיימת, כדי להבטיח שלינק הגישה של האורחים לפורטל **לעולם לא ישתנה**.
